# Requirements

Append-only log of user needs and functional requirements. Written by
`msg-pre-roadmap`, read by `msg-roadmap-plan-item`'s gate-check. Never edit or
delete a row — add new ones.

| Module | Feature | User Need Code | User Need Details | Functional Requirement Code | Functional Requirement Details | Addition Date |
| ------ | ------- | --------------- | ------------------ | ---------------------------- | -------------------------------- | -------------- |
| Session Management | Multi-Session Container | UN.1 | Run several browser idle game accounts at the same time without creating a separate browser profile, guest window or second browser install for each one. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.1.1 | A session represents one game account and is backed by its own `WebKitNetworkSession`, constructed with a data directory and a cache directory unique to that session. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.1.2 | Two sessions of the same game share nothing: cookies, localStorage, IndexedDB, service workers and caches are isolated per session, so both can be logged in simultaneously. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.1.3 | The app runs one web process per live session and relies on WebKitGTK's single global network process; isolating a session must not add a network process. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.2 | Stay logged in to every account indefinitely without the application storing or handling credentials. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.2.1 | The app never reads, stores, or transmits credentials. Login persistence is entirely the network session's cookie store. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.2.2 | Each session's cookie manager is configured with persistent storage so cookies are written to that session's data directory and survive an app restart. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.2.3 | A session's data directory survives destroying and recreating its web view, so parking, unparking and restarting never require logging in again. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.3 | See several games at once, arranged on screen, and change that arrangement without disturbing what is running. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.3.1 | The grid supports three layouts: one full-window slot, two side-by-side slots, and a two-by-two grid of four slots. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.3.2 | Switching to a layout with fewer slots pushes the overflow sessions off-grid without changing their liveness; switching back returns them to the slots they held. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.3.3 | Off-grid live views stay mapped at out-of-bounds coordinates inside a clipping container, because an unmapped view is treated as hidden by WebKit. `GtkStack` is excluded, as it unmaps its non-visible children. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.4 | Reach any account quickly when there are more accounts than the grid has slots. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.4.1 | A collapsible sidebar lists every session with its liveness and visibility state. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.4.2 | Selecting a session in the sidebar swaps it into the focused grid slot, displacing whatever that slot held to off-grid. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.4.3 | Collapsing the sidebar gives the grid the full window width and is reversible without losing grid state. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.5 | Control which accounts consume memory, independently of which accounts are on screen. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.5.1 | Every session carries two independent user-set attributes: liveness, either live or parked, and visibility, either shown in a grid slot or off-grid. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.5.2 | Parking a session terminates its web process before the web view is destroyed, so the memory is returned rather than handed to WebKitGTK's web process cache. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.5.3 | The web context uses a cache model that forces the web process cache capacity to zero, so a terminated process is never retained. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.5.4 | Unparking constructs a new web view bound to the session's existing network session, since the network session is a construct-only property and cannot be rebound. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.5.5 | A parked session reports zero web process memory and reloads on demand without a login prompt. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.6 | Keep a hidden game ticking at full speed when that game loses progress while throttled. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.6.1 | Each session has a keep-awake flag whose initial value comes from its preset and which the user can change per session. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.6.2 | A keep-awake session disables hidden-page timer throttling and hidden-page CSS animation suspension through the WebKit settings feature list. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.6.3 | A keep-awake session injects a document-start user script that routes `requestAnimationFrame` through a timer while the page reports hidden, because WebKit suspends scripted animations unconditionally with no setting to prevent it. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.6.4 | Sessions without keep-awake behave like a background browser tab when off-grid. The documentation states that minimizing the window marks every view hidden regardless of the flag. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.7 | Know what the app is costing in memory, since low memory use is the reason it exists. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.7.1 | The sidebar footer shows the shell's own memory, the number of live sessions, their aggregate memory, and the total, read as proportional set size rather than resident set size so shared library pages are not counted repeatedly. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.7.2 | A script in the repository reports the same figures broken down by process, for use outside the app. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.7.3 | Memory budget thresholds are recorded in the repository and set from a real measurement of at least three games, not assumed in advance. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.7.4 | Per-session memory attribution is out of scope for this feature; only aggregate figures are reported. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.8 | Reopen the app and find the sessions, arrangement and running state exactly as they were left. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.8.1 | The session file persists each session's name, preset, liveness, visibility, slot assignment and keep-awake flag, plus the active layout. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.8.2 | On launch, sessions that were live reload one at a time rather than all at once, so startup does not spike memory or network use. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.8.3 | Configuration and presets live under the XDG config directory and session profiles under the XDG data directory, with no hardcoded paths. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.9 | Not lose a night of idle progress because a game crashed while unattended. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.9.1 | When a session's web process terminates unexpectedly, the app reloads it automatically after one second, then five, then thirty, then two minutes. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.9.2 | After the fourth failed attempt the app stops retrying, the session's slot shows an inline error with the attempt count and a retry control, and the sidebar marks the session as failed. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.9.3 | Retrying by hand resets the backoff, and a session that recovers on its own clears the failed state without user action. | 2026-09-06 |
| Session Management | Multi-Session Container | UN.10 | Add a new account in a few clicks, including for games that reject a non-Chrome browser. | — | — | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.10.1 | Presets ship as hand-editable files in the repository, each holding a display name, a starting URL, a user agent, a zoom level and a default keep-awake value. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.10.2 | Adding a session means choosing a preset and naming the account; the app creates the profile directory and derives the rest from the preset. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.10.3 | A custom entry accepts a name and a URL directly, for any game without a preset. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.10.4 | Each session's user agent is set from its preset, defaulting to a current Chrome string, because WebKitGTK's own user agent causes games to reject the browser. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.2.4 | A sign-in the game opens in a new window — `window.open` or `target="_blank"` — opens as a real top-level window sharing that session's network session and web process, because hosted sign-in completes in a popup and grants its cookie to the opener's session. | 2026-09-06 |
| Session Management | Multi-Session Container | — | — | FR.10.5 | Supersedes FR.10.4. A session's user agent defaults to the engine's own, and a preset overrides it only for a game measured to need a different one. A Chrome string contradicts what WebKitGTK exposes — no `window.chrome`, no `navigator.userAgentData`, `navigator.vendor` of `Apple Computer, Inc.` — and measurement showed that contradiction blocks Google sign-in and Cloudflare Turnstile outright, which is a worse failure than being an unrecognised browser. | 2026-09-06 |
| Session Management | Interactive Zoom | UN.11 | Adjust how large a game's page is drawn while looking at it. The right size depends on the current layout — one full-window slot versus a two- or four-slot grid — and on the game, since two games at the same slot size read best at different zooms. | — | — | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.1 | `Ctrl` `+` / `Ctrl` `=` zooms in one step and `Ctrl` `-` zooms out one step, a step being ×1.1 / ÷1.1 (about ±10%), acting on the account in the focused grid slot. The shortcuts are window-level: active whenever the window is focused and at least one slot is filled, never gated on a web view holding keyboard focus, and a no-op when the grid is empty. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.2 | `Ctrl` `0` resets the focused slot's account to its preset zoom for the current layout, removing any stored override for that layout. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.3 | `Ctrl` + mouse wheel over a view zooms that view by one step per wheel notch and consumes the event so the page does not also scroll. It acts on the view the pointer is over, which is always a visible slot. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.4 | Every zoom the gestures produce is clamped to the existing `ZoomLevel` range, 0.25 to 5.0. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.5 | A zoom change acts on the live view directly with no reload, so the account's liveness is unchanged and it never moves to `Starting`. A parked account has no view and takes no gesture; its stored zoom still updates and its next start applies it. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.6 | On a zoom gesture a transient readout of the resulting percentage appears over the affected view and fades after about one second. Zoom changes driven by a layout switch are silent. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.7 | An off-grid account cannot be zoomed: the keyboard shortcuts act on the focused slot and the wheel needs the pointer over a visible view. It takes its stored or preset zoom for the current layout when next brought into a slot. | 2026-09-07 |
| Session Management | Interactive Zoom | UN.12 | Have each account reopen at the size last chosen for it, remembered separately per layout, so moving between the one-, two- and four-slot arrangements restores the zoom picked for each rather than requiring it to be redone every time. | — | — | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.1 | Zoom is remembered per account and per layout (`Single`, `SideBySide`, `Grid`). A new account has no stored zoom for any layout and follows its preset zoom (`FR.10.1`); a gesture records an override for the current layout only. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.2 | Switching layout snaps each visible account's view to the zoom it last stored for that layout, or to its preset zoom if it has none. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.3 | The stored zoom is a new writable per-account file, `state.toml`, under the account's profile directory in the XDG data location, a sibling of `data/` and `cache/` (`FR.8.3`). It is the application's first writable per-account state and is shaped so a later per-account setting is one more key, not a new file. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.4 | Only a zoom gesture writes `state.toml`. Layout switches, parking, focus changes and shutdown do not. The file holds only the layouts the user has actually changed, and `Ctrl` `0` removes the current layout's entry. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.5 | Writes are best-effort and coalesced: the view updates immediately, a short idle timer writes the settled value once activity stops, and a failed write logs without interrupting the session. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.6 | Reads never fail the application. A missing `state.toml` is normal for a fresh account and means "use the preset zoom". A malformed value is dropped per key — that layout falls back to the preset zoom and logs — while valid entries in the same file still apply, matching `TomlPresetCatalogue`'s tolerance. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.7 | When a view is built for an account it opens at the stored zoom for the current layout, falling back to the preset, so a parked-and-restarted or relaunched account reopens at the chosen size. Durable per-account identity across a relaunch is item 07's concern; until then zoom persists on the same terms as the rest of the profile, through `SessionId` reuse. | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.12.8 | The domain owns the per-account, per-layout zoom map with the preset as its baseline and exposes it through a port — a reader and writer of per-account zoom — implemented outside the core by the TOML and filesystem adapter in `idle-manager-store` and wired in the composition root, matching `ProfileLocator` and `PresetCatalogue`. The core stays free of serde and the filesystem (architecture rule 1). | 2026-09-07 |
| Session Management | Interactive Zoom | — | — | FR.11.8 | The wheel gesture acts only when the view the pointer is over is also the focused slot, narrowing `FR.11.3`: the click is what arms the wheel. Over an unfocused view the event is not consumed, nothing is zoomed and no readout appears. A window holds up to four live views at once, where a browser holds one tab, and a pointer resting over a neighbour must not resize a game nobody is looking at. | 2026-09-07 |
| Session Management | Rearranging Accounts | UN.13 | Correct an account's name after it was created, without destroying the account, logging in again, or losing anything it has on disk. | — | — | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.13.1 | A `Rename…` item in the sidebar row's existing ⋯ menu — beside Park/Start and keep-awake, where a row's settings already live — opens a small dialog with the current name filled in. It changes the account's display name and nothing else. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.13.2 | A rename never touches `SessionId`, which names the account's folder on disk, so the profile directory, its cookie store and its `state.toml` are untouched. Nothing reloads, no liveness changes and no login is required. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.13.3 | The name is validated exactly as the add-game dialog validates it: trimmed, and the confirm button stays insensitive while it is empty. A name duplicating another account's is accepted, because creation already accepts one and rejecting it only at rename would be inconsistent. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.13.4 | The new name appears immediately everywhere the old one was shown — the sidebar row and the name cover the grid draws over a slot until its page paints — and is written through the existing debounced save (`FR.8.1`), adding no second write path. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.13.5 | Renaming works whatever the account's liveness: live, parked, starting or queued. There is no state in which an account can be seen but not renamed. | 2026-09-08 |
| Session Management | Rearranging Accounts | UN.14 | Put an account in the place on screen it should occupy, rather than accepting where the placement pass seated it, and have the account list read like the window afterwards. | — | — | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.1 | Each occupied slot carries a drag grip in its top-right corner, revealed while the pointer is inside that slot and hidden otherwise, so nothing sits over a game that is being watched. The grip belongs to the slot, not the view, so a slot holding a parked or queued account's placeholder has one too. No grip is drawn in the `Single` layout, where there is no second slot to drop on. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.2 | Pressing the grip and dropping on another occupied slot swaps the two accounts' slots, with nothing else moving, so dragging back undoes it. Dropping on an empty slot moves the account there. Dropping on the source slot, or outside every slot, does nothing. The two results are the domain's existing `Outcome::Swapped` and `Outcome::Filled`. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.3 | Grabbing the grip does not focus the slot: arranging an account and looking at one are separate acts, which keeps `FR.11.8`'s rule that a click is what arms `Ctrl`+wheel zoom. The grip's own click gesture claims the event sequence on press and is grouped with its drag source, which denies the sequence to the grid's click-to-focus gesture on the ancestor. The grid's gesture therefore has to stay in the bubble phase. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.4 | Only the grip itself takes pointer input. Any container positioning it over the view is marked non-targetable (`can-target` false, GTK 4's replacement for GTK 3's overlay pass-through), or it would swallow every click meant for the game page. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.5 | The drag carries a private application type, never text, and each slot's drop target runs in the capture phase. `WebKitWebViewBase` installs a drop target of its own, and a text-typed drag is precisely what a web page accepts and pastes into itself; both measures keep an internal drag from ever reaching a game. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.6 | The slot under the drag is highlighted while it is over it. The highlight is driven by a controller whose pointer test includes descendants, because during a drag the pointer is over the web view filling the slot rather than over the slot itself. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.7 | A drop rewrites the account list to read like the window: accounts holding slots first in slot order, then any off-grid accounts keeping their existing relative order. Order stops meaning the order accounts were added and starts meaning where they sit. The new slots and the new order are both written through the existing debounced save. | 2026-09-08 |
| Session Management | Rearranging Accounts | — | — | FR.14.8 | A drag changes where accounts sit and nothing else. No liveness changes, no page reloads and no zoom is recalculated — the layout has not changed, so `FR.12.2`'s snap does not apply. | 2026-09-08 |
| Session Management | Account Workspaces | UN.15 | Keep the accounts that belong together as a named set with its own arrangement — four accounts that play as one party, arranged for that party — instead of one flat list of every account under one window-wide layout. | — | — | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.1 | A workspace is a named set of accounts carrying its own layout, its own slot assignments and its own focused slot. Every account belongs to exactly one workspace, and only the active workspace's accounts occupy slots. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.2 | The existing `Workspace` type — accounts plus layout, already the shape a workspace needs — gains a name and becomes one workspace. A new saved-state type above it holds the list of workspaces and which is active. `sessions.toml` migrates by becoming a single workspace named `Ungrouped`, so an existing installation loses nothing and a user who never makes a workspace sees no change. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.3 | A named workspace holds at most four accounts, the slot count of the largest layout, so no account is off-grid for want of room. `Ungrouped` is exempt: it is the holding pen every account starts in and behaves exactly as the flat list does today. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.4 | `Ungrouped` can be neither renamed nor deleted. Deleting any other workspace returns its accounts to `Ungrouped`; no profile directory is ever removed by deleting a workspace. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.5 | An account may still be off-grid inside a workspace whose layout has fewer slots than the workspace has members — choosing `Single` to look at one game closely is deliberate and reversible, since remembered slots restore the arrangement. Holding more accounts than the largest layout can show is out of scope and needs paging. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.6 | Zoom stays remembered per account per layout (`FR.12.1`). Two workspaces both arranged as `Grid` cannot collide, because an account belongs to exactly one workspace and their account sets are disjoint. | 2026-09-08 |
| Session Management | Account Workspaces | UN.16 | Reach any account from a single index that also says which set it belongs to, without an account appearing twice or a set hiding its members. | — | — | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.16.1 | The sidebar becomes a two-level accordion: workspace headings with their accounts nested beneath. Every account appears exactly once, under its workspace. There is no all-accounts view. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.16.2 | Which workspaces are expanded is remembered per workspace and survives both a sidebar rebuild and a relaunch. The tree model discards a collapsed row's children rather than retaining them, so expansion is state the shell holds and re-applies, not state the model keeps. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.16.3 | Clicking an account belonging to an inactive workspace switches to that workspace and brings the account into its focused slot, in one gesture. Expanding a heading is not itself a switch. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.16.4 | The layout control acts on the active workspace, replacing the window-wide layout. Changing it leaves every other workspace's arrangement alone. | 2026-09-08 |
| Session Management | Account Workspaces | UN.17 | Decide which accounts form a set, and move an account from one set to another, without destroying and rebuilding either. | — | — | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.1 | Account rows carry tick boxes. Ticking is how the user says which accounts an action applies to; it is independent of which account is on screen. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.2 | One action serves both grouping and moving, because they are the same act: `Move to workspace…` lists every workspace with room for the whole selection, plus `Ungrouped`, plus `New workspace…` at the bottom. Choosing the last asks for a name and creates a workspace holding exactly the ticked accounts. A workspace without room is not offered. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.3 | Accounts arriving in a workspace are appended to the bottom of its list; they take no slot from an account already there. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.4 | The ticked set is held as account identifiers by the shell, never as the list view's own positional selection. Collapsing a workspace removes its rows from the tree model and the sidebar rebuilds its store on every sync — positional selection survives neither, and a move that silently skipped a collapsed workspace's accounts would be worse than no move at all. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.5 | The add-game dialog gains a workspace field listing every workspace with room plus `Ungrouped`, defaulting to the active workspace when it has room. Where a new account lands is chosen, never inferred. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.17.6 | A workspace heading carries its own menu holding `Rename…` and `Delete`, the same shape as the account row's ⋯ menu. `Ungrouped`'s menu offers neither (`FR.15.4`). | 2026-09-08 |
| Session Management | Account Workspaces | UN.18 | Move between sets without paying for it — no reload, no login, and no game losing the progress it would lose if it were stopped. | — | — | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.18.1 | Switching the active workspace changes visibility only. The outgoing workspace's accounts go off-grid with their liveness untouched, exactly as `FR.3.2` describes a layout shrinking. Nothing reloads, nothing needs a login, and a kept-awake game keeps running at full speed. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.18.2 | Every live account costs memory whatever workspace is showing. The sidebar footer's figures (`FR.7.1`) count every live account across every workspace, not the active one's. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.18.3 | Which workspace is active is part of the saved state and is restored on launch, so the application reopens showing the set it was left on. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.18.4 | On launch every account that was running is restored, not only the active workspace's, so the first switch after a launch is as free as every other. `start_order` gains an ordering rule — the active workspace's accounts first — so the window is usable while the rest come up behind them, one at a time through the existing start queue (`FR.8.2`). | 2026-09-08 |
| Session Management | Multi-Session Container | UN.19 | Run the application all day and have it cost less memory than the browser it replaced, rather than merely claim to. | — | — | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.1 | The memory figures count every descendant process, not only direct children. WebKitGTK starts each web process inside two nested `bwrap` sandboxes, so the rendering process is a grandchild of the application; a probe matching direct children alone would report the network process and miss the largest figure in the application. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.2 | `docs/memory-budget.md` records the same three games measured on the same machine in both this application and the browser it replaces, and sets the budget from that comparison. Being cheaper than a browser is the project's claim, so it is recorded as a measurement someone else can repeat, not as an absolute figure with no reference. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.3 | A live account's memory is sampled over a soak of at least four hours and the curve recorded, so unbounded growth is distinguished from a large but stable working set. A single reading cannot tell the two apart, and only one of them is a defect. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.4 | The application applies `WebKitMemoryPressureSettings` with a memory limit and conservative and strict thresholds, so the engine sheds caches and collects harder as a web process approaches the limit. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.5 | No kill threshold is set. Killing a web process discards whatever progress the game has not sent to its own server, which is the loss `UN.9` exists to prevent; a process that keeps growing is reported to the user and never killed on the application's own initiative. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.6 | Diagnostics that cost memory in every account are off unless asked for. The web inspector backend and per-resource load logging are enabled by an environment switch or a debug build, never unconditionally in a release run. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.19.7 | WebGL is a per-game preset field defaulting to enabled, so a game that does not use it stops paying for a graphics context while a game that needs it is unaffected. Hardware acceleration is left at the engine's default, which is on wherever the machine supports it. | 2026-09-08 |
| Session Management | Multi-Session Container | UN.20 | Be told when the application has grown past what it budgeted, without it shutting anything down. | — | — | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.20.1 | When the total crosses the budget recorded in `docs/memory-budget.md`, the sidebar footer takes a warning appearance and states that the budget is exceeded. It is a statement of fact, not an error. | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.20.2 | The warning never parks, reloads or otherwise acts on an account by itself. Which account to give up is the user's decision, and per-account attribution does not exist to make it automatically (`FR.7.4`). | 2026-09-08 |
| Session Management | Multi-Session Container | — | — | FR.20.3 | The warning clears on its own as soon as a later sample falls back under the budget, unlike the message strip of design rule 9. It reports a live measurement rather than an event that happened, so a stale warning would be a false one. | 2026-09-08 |
| Session Management | Account Workspaces | — | — | FR.15.7 | A workspace has no state of its own. Its heading carries no status dot or marker; liveness and visibility stay the accounts' own (`FR.5.1`), and a workspace is an organising set, not a thing that runs. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.15.8 | A workspace name is trimmed and must be unique ignoring case, `Ungrouped` included: the confirm button when creating or renaming a workspace stays insensitive while the name is empty or matches another. Unlike account names (`FR.13.3`), because a workspace is chosen by name in the move menu and the add-game dialog, where two identical entries could not be told apart. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.15.9 | A named workspace left with no accounts, by moves or deletions, stays with its name and layout until it is removed by hand. Its heading shows one dim line reading that it holds no accounts. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.15.10 | When the active workspace holds no accounts, the grid shows an empty state reading "No games in this workspace" in place of the slots. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.15.11 | Supersedes the `Delete` label in `FR.15.4` and `FR.17.6`: the workspace heading's action is `Remove workspace`, so it cannot be mistaken for deleting an account (`FR.21.1`), which destroys data. Its behaviour is unchanged — the accounts return to `Ungrouped` and nothing on disk is removed. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.16.5 | The sidebar widens from 150 to 200 pixels, so a nested account row can carry its indentation and, in selection mode, a tick box without its name collapsing to an ellipsis (design rule 6, re-derived by eye). | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.17.7 | Refines `FR.17.1`: tick boxes appear only in selection mode, entered by a `Select` toggle at the top of the sidebar and left by `Done`. In selection mode a click on an account row ticks or unticks it and never switches workspace or focus, the heading menus are hidden, and a bar above the memory footer shows how many accounts are ticked beside `Move to workspace…`. Choosing a destination or pressing `Done` leaves selection mode and clears every tick. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.17.8 | Moving accounts never changes which workspace is active. An account moved out of the active workspace goes out of sight with its liveness untouched, and the slot it held stays empty — no other account slides or fills in behind it. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.17.9 | Refines `FR.17.5`: adding an account into a workspace that is not active does not switch to it. The account is created running inside that workspace, seated in that workspace's own arrangement, out of sight until that workspace is shown. | 2026-09-13 |
| Session Management | Account Workspaces | UN.21 | Delete an account that is no longer played, together with everything it keeps on this computer, from whichever workspace it sits in, without leaving its data behind and without putting any other account at risk. | — | — | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.1 | `Delete account…` is the last item in the account row's ⋯ menu, below `Rename…`, offered for an account in any workspace or in `Ungrouped` and in every liveness: live, parked, starting or queued. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.2 | It opens a confirmation dialog naming the account and stating that its logins and saved data will be removed from this computer and cannot be recovered. The destructive-styled confirm button is the only way forward; cancelling changes nothing. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.3 | Once confirmed, the dialog stays open showing progress until deletion finishes, and it cannot be cancelled or dismissed, because data already cleared cannot be put back. The wait is bounded by a fixed timeout, after which the failure path of `FR.21.6` runs. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.4 | Deletion runs in this order: clear every kind of website data from the account's network session with a timespan of zero and wait for its completion callback; terminate the web process and drop the view, its holder and the network session; wait, polling up to the timeout, until the shared network process holds no open file under the account's profile folder; then remove `<XDG data>/idle-manager/profiles/<id>/` recursively — `data/`, `cache/` and `state.toml` together. Destroying a network session does not wait for the network process to close that session's storage files, so removing the folder straight after would race it. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.5 | On success the dialog closes and the account is gone from its workspace, the sidebar and the grid. The slot it held stays empty, it leaves the ticked set if it was ticked, a queued account is skipped by the start queue, and the change is written through the existing debounced save (`FR.8.1`). | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.6 | If the folder cannot be removed — a file still open when the timeout passes, or a filesystem error — the dialog turns into an error naming the folder and the reason, with `Retry` and `Close`. On `Close` the account stays in its workspace, parked and with its data already cleared, and `Delete account…` retries the whole deletion. No new status is added to the sidebar's vocabulary (design rule 1). | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.7 | A deleted account's identifier is never minted again. The session file records the next identifier to mint rather than deriving it from the highest identifier restored, because deleting the newest account would otherwise hand its folder name to the next account added — inheriting any data a failed removal left behind, and giving WebKit's shared network process two live sessions on one storage path, which it stops on with a release assertion that ends every account at once. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.8 | Deleting an account touches no other account's folder, and removing a workspace deletes no account (`FR.15.4`, `FR.15.11`). The only action in the application that removes a profile folder is `Delete account…`. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.9 | Supersedes `FR.21.3`. Once confirmed, the dialog stays open showing progress until deletion finishes, and it cannot be cancelled or dismissed, because data already cleared cannot be put back. There is no timeout: every step either completes or reports a filesystem error, and measured on WebKitGTK 2.52.6 the whole sequence takes well under a second. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.10 | Supersedes `FR.21.4`. Deletion runs in this order: clear every kind of website data from the account's network session with a timespan of zero and wait for its completion callback; terminate the web process and drop the view, its holder and the network session; wait a fixed quarter of a second; then remove `<XDG data>/idle-manager/profiles/<id>/` recursively — `data/`, `cache/` and `state.toml` together. The application does not wait for the engine to release the folder's files, because it never does: measured on WebKitGTK 2.52.6, the shared network process kept the cookie database, the HSTS store and a file the clear itself creates open for as long as it ran. Removing the folder still succeeds on Linux, nothing recreates it, and every other account's login and storage are unaffected. Each deleted account leaves a handful of unlinked file handles open until the application exits, which is accepted. | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.11 | Supersedes `FR.21.6`. Deletion fails only when removing the folder returns a filesystem error. The dialog then turns into an error naming the folder and the reason, with `Retry` and `Close`. On `Close` the account stays in its workspace, parked and with its data already cleared, and `Delete account…` retries the whole deletion; a folder already gone counts as removed. No new status is added to the sidebar's vocabulary (design rule 1). | 2026-09-13 |
| Session Management | Account Workspaces | — | — | FR.21.12 | A sign-in popup window left open when its account is deleted is not tracked or closed by the application. Its page dies with the account's web process, and deletion neither waits for it nor refuses because of it. | 2026-09-13 |
| Platform Support | Windows Support | UN.1 | Run idle-manager natively on a Windows 10 or 11 PC with the same features it has on Linux, without installing WSL, a Linux virtual machine or any runtime Windows does not already ship. | — | — | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.1 | The application builds and runs natively on 64-bit Windows 10 22H2 and Windows 11 with the MSVC toolchain. GTK 4 stays the interface toolkit on both systems. The web engine is chosen at compile time by target system: WebKitGTK on Linux, Microsoft Edge WebView2 (the Evergreen runtime Windows ships) on Windows. There is no runtime engine choice. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.2 | Every shipped feature of the Session Management module works on Windows: isolated accounts in one-, two- and four-place layouts, the sidebar, parking, keep-awake, the memory footer, presets and adding an account, workspace restore, interactive zoom by keys and by `Ctrl`+wheel, renaming and drag-to-rearrange, workspaces, and deleting an account. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.3 | On Windows every account runs in one shared WebView2 environment with a single user data folder under the application's local data directory, and each account gets its own named engine profile keyed by its session identifier. Two accounts of the same game can be logged in at once and share no cookies, storage, databases, workers or caches. No account gets an environment of its own, because each environment adds a browser process and its helpers. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.4 | On Windows the application's files live where Windows expects them, found through the same platform path lookup Linux uses. Presets, the session file and each account's `state.toml` keep their formats unchanged, so a file copied from one system reads the same on the other. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.5 | Deleting an account on Windows removes the account's engine profile through the engine's own profile-deletion call, then removes the account's folder. Success and failure behave as Session Management `FR.21.9` and `FR.21.11` describe, and no other account's profile or folder is touched. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.6 | A game's engine process ending on Windows — exited, crashed or reported unresponsive — reaches the same handling the Linux build gives a terminated web process: the account's view is dropped and its place shows the stopped state. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.7 | Keyboard shortcuts the application owns, including zoom `Ctrl` `+`/`-`/`0`, work on Windows while a game page has keyboard focus. `Ctrl`+wheel over a game steps the application's own zoom exactly as on Linux; the engine's built-in zoom is switched off so the two never compound. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.8 | Every control the Linux build draws over a game's place — the drag grip, the zoom readout, the loading cover and the drop highlight — stays visible and usable on Windows, even though a WebView2 view draws above anything GTK draws in the same area. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.9 | Keep-awake stays a per-account choice on Windows. An account with keep-awake on keeps running at full rate while the window is minimised or covered. An account with keep-awake off may be slowed by the engine while the window is minimised, as on Linux. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.1.10 | If the WebView2 runtime is missing or cannot start, the application shows a message naming the runtime and where to get it, then exits without touching any account's data. | 2026-09-17 |
| Platform Support | Windows Support | UN.2 | Get the same low running cost on Windows that the project promises on Linux, without the Windows work making the Linux build cost more. | — | — | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.2.1 | The Linux build is unchanged in behaviour and cost: same engine, same processes, and `make memory-report` figures within measurement noise of those recorded in `docs/memory-budget.md`. Windows-only dependencies are not compiled on Linux. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.2.2 | On Windows the memory footer reports the application's cost summed over its own process and every engine process it started, found by walking its process tree and reading each process's private working set, with private committed memory as the fallback on Windows builds that do not report it. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.2.3 | On Windows, four live accounts cost no more memory than the same four games open as tabs in Microsoft Edge, measured the same way as the Linux budget comparison. Idle processor use is no higher than Edge's for the same four games, and pages render with GPU acceleration rather than a software fallback. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.2.4 | On Windows the application starts no process per account beyond the renderer the engine itself assigns to each profile's pages; the browser, GPU and utility processes are shared by every account. | 2026-09-17 |
| Platform Support | Windows Support | UN.3 | Get the application onto a Windows PC simply, and keep the Windows build working while day-to-day development happens on Linux. | — | — | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.3.1 | One command on a Windows machine builds a release and produces a zip holding the executable plus the GTK runtime files it needs. Unzipping and running it needs no installer and no administrator rights. The WebView2 runtime is not bundled. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.3.2 | The Windows code is type-checked and linted from the Linux development machine by a `make` target that `make verify` includes, so a change made on Linux cannot break the Windows build unnoticed. `make bootstrap` installs what that target needs. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.3.3 | The stack and architecture docs name both target systems and both engines, and `make arch-check` forbids the domain, storage and metrics crates from depending on either web engine. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.2.5 | Supersedes `FR.2.3`. On Windows, the memory of four live accounts is compared against Microsoft Edge holding the same four games as tabs, both measured the same way in the Windows development VM, and the application must not cost more. Hardware graphics acceleration and idle processor use are not measured or gated, because the VM has no GPU and draws in software. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.3.4 | Supersedes `FR.3.1`. One `make` target on the Linux development machine cross-builds the Windows release and packages it into a zip. The zip holds the executable plus the GTK runtime files it needs. Unzipping and running it on Windows needs no installer, no administrator rights and no GTK install. The WebView2 runtime is not bundled. No Windows-side build script is kept. | 2026-09-17 |
| Platform Support | Windows Support | — | — | FR.3.5 | Windows behaviour is developed and verified in a Windows 11 virtual machine that `dockur/windows` runs under Docker Engine on the Linux development machine, and acceptance covers only what that VM can show. Every Linux feature still ships on Windows as specified, including behaviour the VM cannot exercise. A developer on a real Windows machine checks that behaviour informally, and the check does not gate acceptance. | 2026-09-17 |
| Remote Access | Phone Operation | UN.1 | Operate an account that is already running on the always-on desktop from a phone, with the game filling the phone's screen, instead of pinch-zooming a desktop-shaped grid over a general remote desktop. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.1 | The phone experience is purpose-built for a six-inch screen. A general remote desktop showing the desktop window is the arrangement being replaced, so it does not satisfy this need even when framed to a single account. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.2 | The phone shows one account at a time, filling its screen, in the layout the game gives a phone-sized viewport. Every game played supports mobile, so laying out for the phone is the game's own work; the application only has to offer it a phone-shaped viewport (`FR.3.1`). | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.3 | From the phone the user can tap and scroll the game page, and a tap lands where it was tapped. There is no keyboard: text entry waits for the desktop. A sign-in popup window opened by a page is not shown on the phone, so signing in is a desktop act. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.4 | The page on the phone is live: animations play, and a tap on something that has just changed lands, with well under a second from the tap to its visible result. A still picture refreshed on a timer or on request does not satisfy this. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.5 | The phone experience runs on Android. iPhone support is wanted if it adds little; if it adds a lot, it becomes a separate roadmap item rather than stretching this one. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.1.6 | The desktop the phone reaches may run Linux or Windows, and the phone experience works against both, as `Platform Support` `FR.1.2` expects of every shipped feature. | 2026-09-19 |
| Remote Access | Phone Operation | UN.2 | Move between every account the desktop holds from the phone, and bring one back to life, without the list stealing screen from the game. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.2.1 | A sidebar on the phone lists every account by name, grouped by workspace as the desktop sidebar groups them, with each account's liveness in the same words the desktop uses: live, parked, starting, queued, or dead when its page has died. No new vocabulary is added (design rule 1). It scales to however many accounts the desktop holds and shows no thumbnails, because a thumbnail costs rendering. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.2.2 | The sidebar hides so the game fills the screen. Choosing an account in it makes that account the one on the phone screen; one account is shown at a time. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.2.3 | From the phone the user can park and unpark an account and start one that has died, with the same effect those actions have on the desktop. The memory footer is not shown on the phone. | 2026-09-19 |
| Remote Access | Phone Operation | UN.3 | Turn the desktop into a phone-shaped single screen, from the desktop or from the phone, so the game is laid out for the phone that will tap it. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.3.1 | Mobile mode is a toggle offered on the desktop and on the phone. While it is on, the desktop is in Single layout at a phone-shaped viewport, and every account gets that viewport, so mobile-aware games lay out for a phone. It is a layout and nothing more: with the window open the desktop renders as it does for any layout, and resting stays what `docs/ditched/02-sleep-mode.md` settled, minimising with keep-awake on. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.3.2 | The phone-shaped viewport matches the attached phone's own screen. When mobile mode is toggled with no phone attached, a fixed default size the application defines is used. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.3.3 | While mobile mode is off, the phone shows nothing but the mobile mode toggle. Turning it on from the phone shows the current account. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.3.4 | Mobile mode does not survive a restart: the application starts in its usual layout. What leaving mobile mode restores on the desktop was not settled here and is a question for the roadmap item. | 2026-09-19 |
| Remote Access | Phone Operation | UN.4 | Keep the always-on machine cool and idle whenever nobody is looking, because rendering games for no one heats the machine and wastes energy. Being looked at from the phone must cost only what is being looked at, and only while it is. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.4.1 | While no phone is attached the machine is in its resting state, minimised with keep-awake on, and this feature adds nothing to what that state costs today. No new resting state, mode or placeholder is introduced; `docs/ditched/02-sleep-mode.md` binds. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.4.2 | While a phone is attached, only the account on the phone screen is rendered. Every other account is exactly as it would be with no phone attached. Switching account on the phone moves the rendering to the newly chosen account. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.4.3 | An account whose keep-awake is off is woken for as long as it is on the phone screen, so the page is live and taps land, and it returns to the background state its engine had the moment the phone moves on or leaves. Its keep-awake flag is not changed; the per-account choice of `Session Management` `FR.6.1` stands. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.4.4 | Attached means the phone experience is in the foreground showing an account. Switching to another app, locking the phone, or opening the sidebar with no account chosen all count as leaving. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.4.5 | A phone that drops without leaving, whether the battery died, the signal was lost or the experience was killed, is treated as gone after a short fixed silence of well under a minute, and rendering stops on its own. A reconnect resumes where it was. | 2026-09-19 |
| Remote Access | Phone Operation | UN.5 | Reach the machine from wherever the user is: the private home Wi-Fi most of the time, someone else's Wi-Fi or a mobile data connection when out. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.5.1 | The phone reaches the desktop from any network with internet access. The home router is not touched: no port is opened and nothing is configured on it. The mechanism that achieves the reach is the roadmap item's choice, not stated here. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.5.2 | On any network path, including a shared or hostile one, nobody in between can read the page or inject a tap, and the phone verifies it is talking to the user's own machine before it shows or sends anything, because the pages are logged into the user's game accounts. | 2026-09-19 |
| Remote Access | Phone Operation | UN.6 | Be certain that only the user's own phone can ever see or drive the accounts, and that even that phone cannot do more than operate them. | — | — | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.6.1 | Exactly one phone is enrolled, and enrolment happens in person at the desktop. Enrolling another phone replaces the first. Every later connection proves it is that phone; anything else gets nothing, not even a prompt. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.6.2 | The enrolled phone can be un-enrolled from the desktop, after which it is refused immediately, including a session in progress. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.6.3 | Even the enrolled phone can cause only what `FR.1.3`, `FR.2.2`, `FR.2.3` and `FR.3.1` allow: tapping and scrolling the page, choosing the current account, parking, unparking, starting, and toggling mobile mode. The desktop refuses everything else regardless of who asks: adding, renaming, regrouping, reordering or deleting an account, removing a workspace, changing keep-awake or any account setting, loading any other site into an account, and changing the desktop's layout or zoom. Management from the phone is a later item, not this one. | 2026-09-19 |
| Remote Access | Phone Operation | — | — | FR.3.5 | Supersedes `FR.3.4`. Mobile mode does not survive a restart: the application starts in its usual layout. Leaving mobile mode restores the last saved desktop state, the layout and preferences as they were before mobile mode was entered. If the window is minimised it stays minimised; only the layout and preferences are restored. | 2026-09-19 |
| Session Management | Keyboard Navigation | UN.22 | See a workspace a screenful at a time, in its own order, without seating each slot by hand — four accounts in the two-slot layout as two pairs, not as a focused slot and a sidebar click per account. | — | — | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.22.1 | A workspace's accounts are read as pages: page `p` is the `k` consecutive accounts at positions `p·k` to `p·k+k−1` of the workspace's own order, where `k` is the layout's slot count (`Single` and `Mobile` 1, `SideBySide` 2, `Grid` 4). The last page may be part-empty; its trailing slots stay empty. `Ungrouped` pages the same way, however many accounts it holds. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.22.2 | The screen always shows exactly one page, slot `i` holding the page's `i`-th account. The book keeps order, layout and the focused account; every seat is derived from those three, so `remembered_slot` and per-account visibility are no longer stored — an existing `sessions.toml` is read once for its order and focused slot and the seating is rebuilt. Refines `FR.3.2`: a layout switch shows the page that holds the focused account, so what was on screen is still on screen or on the page beside it. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.22.3 | Refines `FR.4.2` and `FR.16.3`: clicking an account in the sidebar turns to the page holding it and focuses its slot. Nothing swaps into the focused slot; in `Single` this is exactly the old behaviour. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.22.4 | Supersedes `FR.14.7`'s rewrite of the list: a drop reorders only within the page it happened on — the page's accounts take the order the slots now read in, and every other page is untouched. Moving an account to another page is done in a layout whose page holds both, `Grid` at most; drag-and-drop stays the only way an order changes. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.22.5 | A pager sits in the header bar directly left of the layout toggles: `‹` and `›` as `go-previous-symbolic` / `go-next-symbolic` icon buttons with the `n/m` readout between them in one linked box, tabular figures so the readout does not jitter. It is hidden whenever the workspace has one page. Either arrow turns a whole page, wrapping from the last to the first, and a turn focuses slot 1 of the new page. With a phone attached the phone follows the focus, as it does for any focus change (Remote Access `FR.4.2`). | 2026-09-20 |
| Session Management | Keyboard Navigation | UN.23 | Move to the next account, and to the next workspace, from the keyboard while a game has the keyboard — no reach for the mouse, no sidebar click, from whichever slot or workspace is showing. | — | — | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.23.1 | `Shift`+`Tab` is *next account*: focus steps to the next occupied slot of the page; from the page's last occupied slot it turns to the next page and focuses slot 1, wrapping from the last page to the first. Every press changes the focused account unless the workspace holds one account, when it changes nothing. Walking a workspace of four in the two-slot layout reads A, B, turn, C, D, turn, A. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.23.2 | `Ctrl`+`Tab` is *next workspace*: the next workspace in sidebar order that holds at least one account, wrapping, `Ungrouped` included. It lands on the page and focused slot the workspace was left on (`FR.18.3`); with a single non-empty workspace it changes nothing. Empty workspaces are skipped, never landed on. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.23.3 | Both keys are the window's, not the page's: they go through the one shortcut function the reload and zoom keys already use, in the capture phase, so they fire whatever holds keyboard focus and a game page never sees them — a key stopped in capture is never handed to the web process, so no page-side `preventDefault` can keep it. GTK delivers `Shift`+`Tab` as the keyval `ISO_Left_Tab`, on X11, Wayland and Windows alike, so the match accepts `Tab`, `ISO_Left_Tab` and `KP_Tab` and reads the modifiers with Caps Lock and Num Lock masked out. Auto-repeat is ignored, or a held `Ctrl`+`Tab` would race through every workspace. A page loses `Shift`+`Tab` for backward field focus; accepted, `Tab` alone is untouched. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.23.4 | While the sidebar is in selection mode (`FR.17.7`) both keys are consumed and change nothing: the screen does not move under a move-to-workspace decision. Leaving selection mode restores them. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.23.5 | The two keys work on both engines. On Windows `WebView2`'s `AcceleratorKeyPressed` delivers both — `Tab` "does not map to a character" and `Ctrl` is held — so they reach the same shortcut function through the path roadmap item 12 task 03 built for the reload and zoom keys, widened to admit `VK_TAB` under `Shift` as well as under `Ctrl`, mapped to `gdk::Key::Tab`. Both are marked handled so the page never sees a `keydown`, and the switch itself runs from the main loop's idle, not inside the callback, because the callback runs with the browser process blocked and a switch focuses a different engine host. | 2026-09-20 |
| Session Management | Keyboard Navigation | UN.24 | Park a whole workspace, and start it again, in one act rather than one row menu per account. | — | — | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.24.1 | `Park all` on a workspace heading parks every account in the workspace that is not already parked: a live account at once, a queued one by leaving the start queue, a starting one the moment its page paints, so nothing builds a second view. No confirmation is asked; `Start all` undoes it. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.24.2 | `Start all` unparks every parked account in the workspace through the existing start queue, one at a time (`FR.8.2`), in the workspace's order. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.24.3 | Refines `FR.17.6`: both items sit at the top of the heading menu, above `Rename…`, and `Ungrouped` gains a heading menu holding exactly these two. Each is insensitive, shown greyed, when it would touch no account (design rule 2). | 2026-09-20 |
| Session Management | Keyboard Navigation | UN.25 | Learn the shortcuts from the application itself, without reading its documentation. | — | — | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.25.1 | The pager's tooltip names the key, "Next account (Shift+Tab)", and a workspace heading's tooltip names its, "Next workspace (Ctrl+Tab)", the way the reload button's tooltip already names F5. | 2026-09-20 |
| Session Management | Keyboard Navigation | — | — | FR.25.2 | A shortcuts window lists every shortcut the window has — reload, zoom, next account, next workspace and itself — through GTK's own convention: a `gtk/help-overlay.ui` resource attached to the application window, opened by the `win.show-help-overlay` action GTK registers on `Ctrl`+`?` and by a `Keyboard Shortcuts` item at the end of the ☰ menu. `GtkShortcutsWindow` is deprecated from GTK 4.18 in favour of libadwaita's dialog; the project pins `v4_10` and does not use libadwaita, so it is the right widget until GTK 5. | 2026-09-20 |
