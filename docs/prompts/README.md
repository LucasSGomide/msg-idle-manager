# Prompts

Structured prompts written by the `msg-write-prompt` skill: one file per
prompt, named after its goal.

Nothing here is generated, and no doc or table is derived from these files —
each one is a standalone record, committed with the rest of the repository so
the prompts that shaped the project can be read alongside it.
